<nav class="navbar inverse-navbar" role="navigation">
  <div class="container-fluid header">
    <div class="navbar-header navbar-right">
      <%= link_to 'Home', '/heros#index', :class => 'navbar-brand' %>
      <%= link_to 'Contact Us', '/heros#index', :class => 'navbar-brand' %>
    </div>
    <div class = "login">
      <ul class="nav navbar-nav navbar-right important">
        <% if admin_signed_in? %>
          <li>Logged in as <strong><%= current_admin.email %></strong>.</li>
          <li><%= link_to 'Edit profile', edit_admin_registration_path, :class => 'glyphicon glyphicon-user' %></li>
          <li><%= link_to "Sign Out", destroy_admin_session_path, :method => :delete, :class => 'glyphicon glyphicon-log-out'  %></li>
        <% else %>
          <li><%= link_to " Sign up", new_admin_registration_path, :class => 'glyphicon glyphicon-user'  %></li>
          <li><%= link_to " Login", new_admin_session_path, :class => 'glyphicon glyphicon-log-in'  %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav> 